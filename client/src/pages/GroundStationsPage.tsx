import React from 'react';
import GroundStationSection from '../components/GroundStationSection';

const GroundStationsPage: React.FC = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <GroundStationSection />
    </div>
  );
};

export default GroundStationsPage;
